The R2 presigned upload is being blocked by CORS even though the CORS policy is correctly configured. The issue is likely the endpoint URL being used to generate the presigned URL.

In /api/get-upload-url/route.ts check the S3 client configuration:
1. The endpoint should be https://${ACCOUNT_ID}.r2.cloudflarestorage.com - make sure this is set correctly
2. Add region: "auto" to the S3 client config
3. Make sure forcePathStyle is not set or is set to false
4. Log the full endpoint URL being used so we can verify it

Also check that R2_ACCOUNT_ID in the environment variables on Vercel matches exactly: 2ad46bfd20711383125fb58b4faeeed4