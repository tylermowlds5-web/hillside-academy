Add the following quiz question types to the quiz builder:

1. TRUE/FALSE - simple two option true or false question, shows two buttons or radio options for True and False

2. MULTIPLE CHOICE - current type, keep as is but make sure it is clearly labeled as Multiple Choice when adding a question

3. IMAGE QUESTION - admin can upload an image (stored to R2) as part of the question. Employee sees the image above the answer choices. Great for plant ID, equipment ID, weed identification tests. Use the existing R2 upload infrastructure for the image.

4. MULTIPLE SELECT - employee can select more than one correct answer (checkbox style instead of radio buttons). Admin can mark multiple options as correct.

5. SHORT ANSWER - employee types a short text answer. Admin sets the correct answer text and it checks for a match case insensitive and ignoring extra spaces.

Update the quiz builder UI so admin can pick the question type from a dropdown when adding each question. Each type shows the appropriate input fields. Update the quiz taking UI to render each question type correctly. Make sure all question types save and score correctly.