Fix these mobile issues:

1. REMOVE CAPTURE ATTRIBUTE - remove capture="environment" from the video file input so mobile users get a choice between camera and photo library instead of opening camera directly

2. DISABLE ZOOM - add this meta tag to the root layout to prevent pinch zoom on mobile:
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" />

3. MOBILE MENU NAVIGATION - when clicking a menu item on mobile the sidebar closes and briefly shows the previous page before loading the new one. Fix this so navigation feels instant - when a menu item is clicked close the sidebar and navigate simultaneously without showing the previous page content in between

4. PROGRESS REPORT ALWAYS HIGHLIGHTED - the Progress Report menu item in the sidebar is always showing as active/highlighted green even when you are on a different page. Fix the active state detection so only the current page's menu item is highlighted green