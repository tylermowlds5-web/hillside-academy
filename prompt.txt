Fix these three issues:

1. EMAIL VERIFICATION REDIRECT - the confirmation email link is pointing to localhost instead of the production URL. In Supabase the email redirect URL needs to be updated. Also in the app make sure the auth callback route at /auth/callback is correctly handling the email confirmation and redirecting to /dashboard after verification. The production URL is https://hillsideacademy.com (or the current Vercel URL until the domain is set up).

2. MOBILE LAYOUT - fix horizontal scrolling on mobile. The app should not scroll side to side on phones. Fix these issues:
   - Add overflow-x: hidden to the root layout
   - Make all tables scroll horizontally within their container instead of pushing the page wider
   - Make the sidebar collapse to a hamburger menu on small screens
   - Make all cards and grids stack vertically on mobile
   - Make buttons and tap targets at least 44px tall for easy tapping
   - Test all main pages: login, dashboard, videos, watch page, learning paths

3. MOBILE VIDEO UPLOAD - video upload is failing on mobile. Fix the upload to work on mobile browsers:
   - Accept camera recorded videos on mobile (add capture attribute)
   - Make sure the file input accepts video files from the phone's camera roll and file system
   - The upload progress bar should work on mobile
   - Handle larger file sizes that mobile videos can produce
   - Make the upload button easy to tap on mobile