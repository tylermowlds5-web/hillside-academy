Fix these mobile issues:

1. MOBILE VIDEO UPLOAD 413 ERROR - Vercel has a 4.5MB request body limit which is too small for mobile videos. Switch to presigned direct-to-R2 uploads:
   - Create an API route /api/get-upload-url that generates a presigned PUT URL for R2 using @aws-sdk/client-s3 and getSignedUrl
   - The upload form should PUT the file directly to R2 using the presigned URL instead of going through Vercel
   - This bypasses the Vercel size limit entirely - videos upload straight from the browser to R2
   - Keep the progress bar working during upload
   - After the direct upload completes, save the video record to Supabase as before

2. MOBILE CATEGORY CREATION - on mobile the "New category..." option in the category dropdown during video upload does not show the text input to type a new category name. Fix this so it works the same as desktop - selecting "New category..." shows a text input field to type a new category name on mobile as well.

3. On iPhone the layout is broken - grey cards/boxes are overflowing off the right side of the screen. Fix this for all pages:
   - Make sure every card, box, and container has max-w-full and does not overflow the viewport width
   - Add w-full to all card components so they fill their container without exceeding it
   - Make sure padding and margins on mobile dont push content off screen - use px-4 on mobile containers
   - Check the dashboard, videos page, learning paths, and admin pages specifically
   - Any grid layouts should be grid-cols-1 on mobile screens
   - Make sure the main content area accounts for the sidebar width correctly - on mobile the sidebar should be fully hidden and the main content should use the full width
   - Test all pages at 390px width (iPhone 14 size)